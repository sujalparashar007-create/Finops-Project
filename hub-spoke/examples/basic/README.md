# hub-spoke - examples/basic

Minimal example of the hub-spoke main module. Creates a hub VPC plus two spoke VPCs (dev and staging) connected via Network Connectivity Center (NCC), with an allow-ssh firewall rule on the hub VPC.